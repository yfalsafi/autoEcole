{% extends 'base.html.twig' %}

{% block title %} {% endblock %}

{% block body %}
    <section>
        <div class="container text-center align-content-center">
            <table id="dataTable" class="table table-striped table-bordered table-sm" cellpadding="12">
                <thead class="thead-dark">
                <tr>
                    <th cope="col">Produit</th>
                    <th class="mr-4" scope="col">Prix</th>
                    <th class="mr-4" scope="col">Date</th>
                </tr>
                </thead>
                {% for purchase in purchases %}

                        <tr>

                            <td class="mr-4"><a href="{{ path('produit_list', {id: purchase.id}) }}">{{ purchase.package.title }}</a></td>
                            <td class="mr-4"><a href="{{ path('produit_list', {id: purchase.id}) }}">{{ purchase.package.price }}€</a></td>
                            <td class="mr-4"><a href="{{ path('produit_list', {id: purchase.id}) }}">{{ purchase.buyAt |date('d/m/Y') }}</a></td>

                        </tr>

                {% endfor %}
            </table>
        </div>
    </section>


{% endblock %}
