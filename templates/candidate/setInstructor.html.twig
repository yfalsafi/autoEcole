{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <section>
        <div class="container">
            <h3>Choisir son Moniteur</h3>
            {{ form_start(form) }}
            {{ form_row(form.name, {}) }}
            <button type="submit" class="btn btn-primary"> C'est lui et pas un autre !!</button>
            {{ form_end(form) }}
        </div>
    </section>

{% endblock %}
